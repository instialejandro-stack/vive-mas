# Descargables

Coloca aqui futuros archivos gratuitos como PDF, PNG o plantillas.

Despues, anade el recurso en `data/resources.ts` usando:

```ts
downloadUrl: "/downloads/nombre-del-archivo.pdf"
```

Mientras no exista el archivo, deja el recurso sin `downloadUrl` y usa la biblioteca como vista previa.
